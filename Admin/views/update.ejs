<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Article</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Roboto", sans-serif;
    }

    body {
      font-family: Arial, sans-serif;
      background: #ffffff;
      margin: 0;
      color: #1e3a8a;
    }

    .navber {
      height: 64px;
      background: #ffffff;
      border-bottom: 1px solid #e0e0e0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0 24px;
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .navber p {
      font-family: "Eater", serif;
      font-size: 1.5rem;
      color: #3a3a3a;
      white-space: nowrap;
    }

    .navber-div {
      display: flex;
      gap: 12px;
      align-items: center;
    }

    .navber-div a {
      background-color: #3b82f6;
      color: white;
      padding: 8px 16px;
      border: 2px solid #3b82f6;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.2s ease;
    }

    .navber-div a:hover {
      background-color: #ffffff;
      color: #3b82f6;
      border-color: #3b82f6;
    }

    .menu-toggle {
      display: none;
      background: none;
      border: none;
      font-size: 1.8rem;
      cursor: pointer;
      color: #3a3a3a;
    }

    h1 {
      text-align: center;
      color: #1e40af;
      margin-bottom: 30px;
      margin-top: 20px;
      font-size: 32px;
    }

    .form {
      max-width: 700px;
      margin: auto;
      background: #ffffff;
      padding: 30px;
      border-radius: 10px;
      border: 1px solid #dbeafe;
      box-shadow: 0px 0px 12px rgba(30, 64, 175, 0.1);
    }

    label {
      font-weight: 600;
      color: #1e3a8a;
      margin-top: 20px;
      display: block;
    }

    .input-form,
    textarea {
      width: 100%;
      padding: 12px;
      margin-top: 8px;
      border: 1px solid #93c5fd;
      border-radius: 8px;
      outline: none;
      transition: 0.2s;
      font-size: 15px;
      color: #1e3a8a;
      background: #f8fafc;
    }

    .input-form:focus,
    textarea:focus {
      border-color: #2563eb;
      box-shadow: 0 0 5px rgba(37, 99, 235, 0.3);
      background: #ffffff;
    }

    textarea {
      resize: vertical;
    }

    .input-div {
      margin-top: 20px;
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 600;
    }

    .input-div input {
      transform: scale(1.2);
    }

    .input-form-sub {
      width: 100%;
      padding: 14px;
      margin-top: 25px;
      background: #2563eb;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 18px;
      cursor: pointer;
      transition: 0.2s;
    }

    .input-form-sub:hover {
      background: #1d4ed8;
    }

    @media (max-width: 768px) {
      .menu-toggle {
        display: block;
      }

      .navber-div {
        display: none;
        flex-direction: column;
        width: 100%;
        background: #ffffff;
        position: absolute;
        top: 64px;
        left: 0;
        border-top: 1px solid #e0e0e0;
        padding: 16px 0;
        height: 95vh;
      }

      .navber-div a {
        width: 90%;
        margin: 6px auto;
        text-align: center;
      }

      .navber-div.active {
        display: flex;
      }
    }
  </style>

</head>
<body>
    <%- include("partials/header") %>

  <h1>Update Article</h1>

  <form action="/admin/update/<%= blog[0].id %>" method="post" class="form">

    <label for="title">Enter Title</label>
    <input 
      type="text"
      name="title" 
      id="title"
      class="input-form"
      value="<%= blog[0].title %>"
      required
    >

    <label for="article">Enter Article</label>
    <textarea 
      rows="20" 
      name="article" 
      id="article"
      required
    ><%= blog[0].article %></textarea>

    <label for="status">Enter Status</label>
    <input 
      type="text"
      name="status" 
      id="status" 
      class="input-form"
      value="<%= blog[0].status %>"
      required
    >

    <label for="imageurl">Enter Image Url</label>
    <input 
      type="text"
      name="imageurl" 
      id="imageurl"
      class="input-form"
      value="<%= blog[0].imageurl %>"
      required
    >

    <div class="input-div">
      <input 
        type="checkbox"
        name="check"
        id="check"
        <% if (blog[0].ismain) { %> checked <% } %>
      >
      Make This Article Main
    </div>

    <input 
      type="submit" 
      value="Update" 
      class="input-form-sub"
    >

  </form>

    <script>
        document.querySelector('.menu-toggle').addEventListener('click', () => {
        document.querySelector('.navber-div').classList.toggle('active');
      });
    </script>
</body>
</html>
